import React from 'react';

// import Navbar from './NavBar';
// import Footer from './Footer';

const Root = (props) => {
    return (
        <div id="main-content-anchor">
            { props.children }
        </div>
    );
};

export default Root;

